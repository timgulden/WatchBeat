import Foundation

/// Orchestrates the full DSP pipeline: condition -> estimate period -> build template
/// -> locate ticks -> analyze rate.
public struct MeasurementPipeline {

    public init() {}

    // TODO: Wire up all stages into a single entry point
}
