import Foundation
import Accelerate

/// Folds raw signal at the detected period to build an averaged tick template.
public struct TemplateBuilder {

    public init() {}

    // TODO: Implement signal folding + averaging + normalization
}
